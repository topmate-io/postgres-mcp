steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/${_IMAGE_NAME}:latest', '-f', 'Dockerfile.aws', '.']
images:
  - 'gcr.io/$PROJECT_ID/${_IMAGE_NAME}:latest'
timeout: '900s'
substitutions:
  _IMAGE_NAME: 'postgres-mcp'
