apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: postgres-mcp
  labels:
    app: redis
    app.kubernetes.io/part-of: topmate-mcp
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
