apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-mcp-v2-ledger
  namespace: postgres-mcp
  labels:
    app: postgres-mcp-v2-ledger
    app.kubernetes.io/name: postgres-mcp-v2-ledger
    app.kubernetes.io/part-of: topmate-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-mcp-v2-ledger
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      annotations:
        karpenter.sh/do-not-disrupt: "true"
      labels:
        app: postgres-mcp-v2-ledger
        app.kubernetes.io/name: postgres-mcp-v2-ledger
    spec:
      serviceAccountName: postgres-mcp-sa
      containers:
        - name: postgres-mcp
          image: 072528252688.dkr.ecr.ap-south-1.amazonaws.com/topmate-postgres-mcp:latest
          args:
            - "--transport=sse"
            - "--sse-host=0.0.0.0"
            - "--sse-port=8000"
            - "--access-mode=restricted"
          ports:
            - containerPort: 8000
              protocol: TCP
          env:
            - name: DATABASE_URI
              valueFrom:
                secretKeyRef:
                  name: postgres-mcp-v2-secrets
                  key: v2-ledger-database-uri
                  optional: true
            - name: ACCESS_MODE
              value: "restricted"
            - name: TRANSPORT
              value: "sse"
            - name: PORT
              value: "8000"
            - name: ALLOWED_IPS
              valueFrom:
                configMapKeyRef:
                  name: postgres-mcp-allowlist
                  key: allowed-ips
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      terminationGracePeriodSeconds: 30
